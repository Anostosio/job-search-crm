{
  "name": "anostosio-job-search-crm",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check privacy.js && node --check scripts/build.mjs && node --test"
  },
  "engines": {"node": ">=20"},
  "dependencies": {
    "@fontsource-variable/manrope": "5.3.0",
    "@fontsource-variable/unbounded": "5.3.0"
  }
}
